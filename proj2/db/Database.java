package db;

import java.util.ArrayList;

public class Database {
    //ArrayList<Table> tables =

    public Database() {
        // YOUR CODE HERE
    }

    public String transact(String query) {
        /*Parser p = new Parser();
        Object[] input = p.eval(query);
        String cmd = input[0]

        if (cmd.equals("create new")) {
            String name = (String)input[1];
            String[] column = (String[])input[2];
            tables.add(new Table(name, column));

        } else if (cmd.equals("create selected")) {
            String name = (String)input[1];
            String[] expressions = (String[])input[2]
            String[] columns = (String[])input[3];
            String[] tables = (String[])input[4];
            tables.add( //add the result of calling the select method, which should be in Database
        } else if (cmd.equals("load")) {


        } else if (cmd.equals("store")) {

        } else if (cmd.equals("drop")) {

        } else if (cmd.equals("insert")) {

        } else if (cmd.equals("print")) {

        } else if (cmd.equals("select")) {

        }*/
        return "";
    }
}
